import 'package:flutter/cupertino.dart';

class ProductCategoryTile extends StatelessWidget {
const ProductCategoryTile({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}